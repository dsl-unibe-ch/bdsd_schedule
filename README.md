# Bern Data Science Day schedule website

Uses [eleventy.js](https://www.11ty.dev/)

## Usage

Install dependencies with `npm install`

Compile static site with `npx @11ty/eleventy`

Serve from `_site`