<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or (renderData and renderData.title) }}</title>
    <meta name="description" content="{{ (description or (renderData and renderData.description)) | default("Bern Data Science Day 2023") }}">
    <meta property="og:title" content="{{ og_title or (renderData and renderData.og_title) or title or (renderData and renderData.title) }}" />
    <meta property="og:description" content="{{ (description or (renderData and renderData.description)) | default("Bern Data Science Day 2023") }}">
    <meta property="og:site_name" content="BDSD 2023" />
    <meta property="og:url" content="{{ constants.baseURL }}{{ page.url }}" />
    <meta property="og:image" content="{{ constants.baseURL }}/img/icon-192.png" />
    <meta name="theme-color" content="#581c87">
    <meta name="author" content="Data Science Lab">
    <link rel="icon" href="/favicon.ico" type="image/x-icon" sizes="any">
    <link rel="icon" href="/img/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/img/apple-touch-icon.png" sizes="180x180">
    <link rel="manifest" href="/manifest.webmanifest">
    <link rel="sitemap" type="application/xml" title="Sitemap" href="/sitemap.xml">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Montserrat:wght@100..900">
    <link rel="stylesheet" href="/css/bdsd.css">
  </head>
  <body class="bg-gradient-to-b from-orange-200 to-white dark:from-purple-950 dark:to-black bg-fixed print:bg-white">
    {{ content | indent(4) | safe }}
  </body>
</html>
